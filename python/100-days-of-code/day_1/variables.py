username = input("What is your name? ") # this will ask the user for their name
length = len(username) # this will get the length of the username
print(length) # this will print the length of the username

